import StrategyRunner from 'pusher-js/types/src/core/strategies/strategy_runner';
interface Strategy {
    isSupported(): boolean;
    connect(minPriority: number, callback: Function): StrategyRunner;
}
export default Strategy;
