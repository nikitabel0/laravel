import URLLocation from 'pusher-js/types/src/core/http/url_location';
import State from 'pusher-js/types/src/core/http/state';
import Socket from 'pusher-js/types/src/core/socket';
import SocketHooks from 'pusher-js/types/src/core/http/socket_hooks';
import HTTPRequest from 'pusher-js/types/src/core/http/http_request';
declare class HTTPSocket implements Socket {
    hooks: SocketHooks;
    session: string;
    location: URLLocation;
    readyState: State;
    stream: HTTPRequest;
    onopen: () => void;
    onerror: (error: any) => void;
    onclose: (closeEvent: any) => void;
    onmessage: (message: any) => void;
    onactivity: () => void;
    constructor(hooks: SocketHooks, url: string);
    send(payload: any): boolean;
    ping(): void;
    close(code: any, reason: any): void;
    sendRaw(payload: any): boolean;
    reconnect(): void;
    onClose(code: any, reason: any, wasClean: any): void;
    private onChunk;
    private onOpen;
    private onEvent;
    private onActivity;
    private onError;
    private openStream;
    private closeStream;
}
export default HTTPSocket;
