import RequestHooks from 'pusher-js/types/src/core/http/request_hooks';
import Ajax from 'pusher-js/types/src/core/http/ajax';
import { default as EventsDispatcher } from 'pusher-js/types/src/core/events/dispatcher';
export default class HTTPRequest extends EventsDispatcher {
    hooks: RequestHooks;
    method: string;
    url: string;
    position: number;
    xhr: Ajax;
    unloader: Function;
    constructor(hooks: RequestHooks, method: string, url: string);
    start(payload?: any): void;
    close(): void;
    onChunk(status: number, data: any): void;
    private advanceBuffer;
    private isBufferTooLong;
}
