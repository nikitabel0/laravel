import { default as EventsDispatcher } from 'pusher-js/types/src/core/events/dispatcher';
import Pusher from 'pusher-js/types/src/core/pusher';
import { PusherEvent } from 'pusher-js/types/src/core/connection/protocol/message-types';
import { ChannelAuthorizationCallback } from 'pusher-js/types/src/core/auth/options';
export default class Channel extends EventsDispatcher {
    name: string;
    pusher: Pusher;
    subscribed: boolean;
    subscriptionPending: boolean;
    subscriptionCancelled: boolean;
    subscriptionCount: null;
    constructor(name: string, pusher: Pusher);
    authorize(socketId: string, callback: ChannelAuthorizationCallback): void;
    trigger(event: string, data: any): boolean;
    disconnect(): void;
    handleEvent(event: PusherEvent): void;
    handleSubscriptionSucceededEvent(event: PusherEvent): void;
    handleSubscriptionCountEvent(event: PusherEvent): void;
    subscribe(): void;
    unsubscribe(): void;
    cancelSubscription(): void;
    reinstateSubscription(): void;
}
