import TimedCallback from 'pusher-js/types/src/core/utils/timers/timed_callback';
import { Delay, Scheduler, Canceller } from 'pusher-js/types/src/core/utils/timers/scheduling';
declare abstract class Timer {
    protected clear: Canceller;
    protected timer: number | void;
    constructor(set: Scheduler, clear: Canceller, delay: Delay, callback: TimedCallback);
    isRunning(): boolean;
    ensureAborted(): void;
}
export default Timer;
